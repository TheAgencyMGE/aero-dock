[package]
name = "aero-dock"
version = "1.3.0"
description = "Aero Dock - a free RocketDock alternative for Windows, in Frutiger Aero"
authors = ["Ryan Panda"]
license = "MIT"
repository = "https://github.com/TheAgencyMGE/aero-dock"
homepage = "https://github.com/TheAgencyMGE/aero-dock"
readme = "../README.md"
edition = "2021"

[lib]
name = "aero_dock_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon", "image-png", "protocol-asset"] }
tauri-plugin-opener = "2"
tauri-plugin-autostart = "2"
tauri-plugin-single-instance = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
parking_lot = "0.12"
log = "0.4"
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "bmp"] }

# The WebView2 bindings Tauri itself is built on, pinned to the same
# versions wry already pulls in, so nothing new is downloaded. Needed to
# reach ICoreWebView2_19, which Tauri does not wrap.
[target.'cfg(windows)'.dependencies]
webview2-com = "0.38"
windows-core-061 = { package = "windows-core", version = "0.61" }

[target.'cfg(windows)'.dependencies.windows]
version = "0.62"
features = [
  "Win32_Foundation",
  "Win32_System_Com",
  "Win32_System_Com_StructuredStorage",
  "Win32_System_Ole",
  "Win32_System_Power",
  "Win32_System_Registry",
  "Win32_System_Threading",
  "Win32_System_ProcessStatus",
  "Win32_System_Variant",
  "Win32_Storage_FileSystem",
  "Win32_UI_Shell",
  "Win32_UI_Shell_Common",
  "Win32_UI_Shell_PropertiesSystem",
  "Win32_UI_WindowsAndMessaging",
  "Win32_UI_Input_KeyboardAndMouse",
  "Win32_UI_Accessibility",
  "Win32_UI_HiDpi",
  "Win32_Graphics_Gdi",
  "Win32_Graphics_Dwm",
  "Win32_Media_Audio",
  "Win32_System_SystemInformation",
  "Media_Control",
  "Foundation",
  "Win32_Media_Audio_Endpoints",
  "Win32_Networking_NetworkListManager",
  "Win32_Devices_FunctionDiscovery",
]

[profile.release]
lto = true
codegen-units = 1
strip = true
opt-level = 3
